import React from 'react';

const GoogleMap = (props) => {
  let map = null;
  const initMap = () => {
    map = new GoogleMap.maps.Map(document.getElementById('map')), {

    }
  }
}

export default GoogleMap;
